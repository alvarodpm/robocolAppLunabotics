import 'package:flutter/material.dart';

class ControlPad extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Aquí van todos los widgets'),
      ),
    );
  }
}
